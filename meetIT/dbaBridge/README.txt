To use the database bridge you will need to make sure the ojdbc is installed in the jdk that your ide is running off of. 

Install it in the jdk's ext folder which should be something like jdk/jre/ext

 